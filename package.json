{
  "name": "ai-character-index",
  "private": true,
  "packageManager": "pnpm@11.26.0",
  "engines": {
    "node": ">=22.13 <25"
  },
  "scripts": {
    "prebuild": "rm -rf public && mkdir -p public && cp -R site/. public/",
    "predev": "rm -rf public && mkdir -p public && cp -R site/. public/",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test:routes": "node --test app/lib/__tests__/*.test.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "mcp-handler": "^2.1.1",
    "next": "^15.5.4",
    "next-auth": "5.0.0-beta.32",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^4.6.2"
  }
}
