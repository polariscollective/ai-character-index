# Closeout list

> **A dated record, not a current to-do list (as of September 2026).** This list tracked the August 2026 closeout of the reader and its clone-and-fork pathway. Much of what it names has since gone: the index moved into Supabase and the site to Vercel; `data/`, the committed runlogs, the reader's manifest and the `specs/` mirrors left the repository; and the clone-and-fork pathway gave way to `engine/local_run.py` and the admin portal. Its open items were not re-triaged. The current state is in `SYSTEM.md` and `CLAUDE.md`.

> **Aim (repo-owner decision):** ship the **model spec reader** — behaviour × model-spec coverage with cited passages — as a small, honest, **fixed demo**, plus a **clear pathway for people to clone/fork the repo and run it on their own behaviours**. The eval-discovery/quality workflow is out of scope and gets stripped. The target is the reader + pathway (Phase 2 below), **timeboxed to this week and likely to land as an incompletely-tested end state**; on top of it, be **explicit about the limitations** so people can judge whether and how to build on it. The full modular platform (Phase 3) stays deferred. Current state is documented in `SYSTEM.md` and the per-directory `OVERVIEW.md` files.
>
> Three **cumulative** phases, ordered safest-first; each leaves the repo coherent and honest, and later phases build on earlier ones. **Phase 2 is the current target.**

## Phase 1 — Tier 3: honest, clean demo (bounded, behavior-preserving changes only)

> **Product if you stop here:** a read-only demo — visitors can read the example coverage (two lab specs × the example behaviours) and see what the project does, but cannot produce their own data without editing code; the only contribution route is the contact channel.
> **Risk: low.** Docs, bounded deletions/hygiene, and behavior-preserving code-quality work (tests, determinism, de-duplication); no interface or behavior changes. The main failure mode is cleanup scope-creep.

- [x] Merge prep PRs **#22** (gitignore + cruft) and **#24** (dead panel code) — done
- [ ] Merge **#23** (docs + this list), then **#25** (intake forms, split out of #23), then **#26** (Phase-1 cleanup)
  - Note: between #23 merging and #25 merging, the README Contributing section still names the eval-submission/appeal channels the scope ruling retires — a transient contradiction that #25 closes.
- [ ] **Honest status note** (README or a top-level note): what this is (a spec reader + a toolkit to clone/fork), what works today vs. what is aspirational/unproven
- [ ] **PLAN.md ruling:** rewrite as a living architecture doc, mark historical, or surgically update — the largest doc/reality gap (§8 lists `outreach/` which is gitignored and never on main; §2/§8 claim schema-enforced CI that doesn't exist; §8's engine row omits `panel/` and the builders; §1.2 promises four workflows where one exists)
- [ ] **`site/methodology.html` + `methodology/site-copy-how-we-assess-coverage.md`:** describe the term-list method while the operative procedure is the LLM panel; the panel method needs writing for the public page
- [ ] **Bounded hygiene:** gitignore the panel runtime artifacts (`engine/panel/runlog*.jsonl`, `metrics.jsonl`, `wholedoc-FAILED-*.txt`). (The committed `.pyc` and the 0-byte archives are already removed by #22; the `runDate` and nav-anchor fixes live in the engineering items below.)
- [ ] **Turn on GitHub "automatically delete head branches"** so merged branches stop accumulating
- [ ] **Parked local branches ruling:** keep/delete `backup/pre-final`, `backup/pre-linearize`; `feat/cite-sweep` suite home (its tests land via W1 in **#26**)
- [ ] **Publishing decisions:** public name/domain; preview deploys for PRs (if wanted)
- [ ] **Phase-1 engineering** — parallel, behavior-preserving, test-driven; each in its own commit with an adversarial review (correctness + test-quality/mutation + Phase-1-goal-consistency):
  - [x] **W1:** land the parked cite.py test suite (`tests/cite-suite`) on main + wire a runner; verify green against current `cite.py` (the foundation Phase 2 will generalize) — landed in PR **#26** (open; `tests/test_cite.py`); superseded-and-extended by `tests/test_cite_user_specs.py` in PR #33/#34 (open)
  - [x] **W2:** rescoped into the Phase-2 **timestamped run outputs** item: per-run timestamped files replace the fixed output destination; the `runDate` determinism piece survives there as content metadata — landed in PR #32 (open; `--run-date=` pins it for reproducible rebuilds, PR #34)
  - [x] **W3:** fix the spec-reader nav anchors (`../#methodology`, `../#about`); make `verify-spec-reader.mjs` fail on unresolved anchors — landed in PR **#26** (open; the verifier now fails on any unresolved same-site link/fragment). The walker was retired with the index reader; nav coverage lives in `verify-reader-test.mjs`
  - [x] **W4:** JSON schemas + validation for `data/*.json` *(optional)* — landed in PR **#26** (open; `data/schema/` + `engine/validate_data.py` + its test suite)
  - [x] **W5:** de-duplicate `coverage_payload()` across `build-spec-reader-data.py` / `build-reader-test-data.py` — done: both builders now import the shared `engine/coverage_payload.py`, so the former duplicate is gone
  - [x] **W6:** rescoped into the Phase-2 **timestamped run outputs** item: with one file per run, forgetting `--runlog=` can no longer append verdicts to the wrong run's file — landed in PR #32 (open)
  - [x] **W7:** fix the stale "unused legacy" comment on `display.threshold` in `panel-config.json` (it is read by `build_site_data.py`) — landed in the panel-packaging work, PR #33 (open); `solid_threshold` bakes into the payload's `adjacent` flag
- [ ] **Documentation: don't route demo users into the internal workflow** — ensure nothing tells a demo user to get data changes reviewed/pushed; the contribution route stays contact-only (the fuller "strip vision/review-workflow docs" is Phase 2)
- [x] **Scope deletions** *(ruled 2026-08-18)*: out-of-scope material (`data/evals.json`, `research/sources/`, sweep stages 1–3 artifacts) deleted via **#29**; the `site/index.html` evidence lens awaits the deferred site-surfaces ruling. `.claude/skills/` scope pass landed via **#30** (deleted `1-sweep-discover/`, `2-sweep-curate/`, `3-sweep-score/` + `references/exclusion-criteria.md`; rescoped `5-sweep-publish/` and `6-sweep-verify/` to coverage-only per the drafts in `docs/proposals/`; retired the `behaviour-sweep/` orchestrator; stripped Notion IDs from `references/locations.md`). Site-surfaces ruling (keep `spec-reader/` + `llm-panel-review/` + test bench? retire the `index.html` prototype?) settled 2026-08-25 by the reader consolidation (`feat/reader-consolidation`): the panel-judged reader is the one `spec-reader/`, the test bench is retired; the `index.html` prototype was retired the same day (root-redirect ruling; the reader is the landing surface). `behaviours-for-adria/` stays in but gets renamed (new name Andrés's call; recommendation `external-review-batch/`). `labs.json` deferred — fold into the spec-identity consolidation later.

## Phase 2 — Tier 2: decoupled single-user toolkit (adds to Phase 1)

> **Product if you stop here:** a user can supply their own spec + behaviours and run the pipeline to produce and view their own coverage data — no new code, nothing pushed back — but it is not yet a general extension platform.
> **Risk: medium.** The decoupling touches load-bearing code whose contract is pinned only for the two bundled specs (the cite.py golden suite lands in **#26**), and the seams get chosen without real users; real risk of breaking the working demo.

- [x] **Generalize `cite.py`'s spec input** — done (#33/#34): user-spec manifest (`specs/user/specs.json`, gitignored; `SPEC_CITE_USER_SPECS` overrides) with `title`/`sourceUrl` rendering metadata; bundled specs unchanged; contracts pinned by `tests/test_cite_user_specs.py`
- [x] **Behaviour-identity single source:** done (#28): `data/behaviours.json` registry (slug-keyed, ids namespaced per set); `engine/generate_behaviour_constants.py` regenerates `BEHAVIOURS` in `build-spec-reader-data.py` and the panel slug lists (the `GROUPS` output for the old index reader's `app.js` went with that app); display-time renumbering replaced by registry `numeric_id`s; drift-gated by `tests/test_behaviour_registry.py`
- [x] **Parameterize the builders** — done for the seam that matters (#34): `build-spec-reader-data.py --user-manifest=` folds user-registered specs into `documents.json` (byte-identical with no manifest); the panel builder takes `--registry=`/`--run-date=`. Residual: the index `BEHAVIOURS` list (ids 1–3) remains hardcoded in the reader builders; `build-reader-test-data.py` is unparameterized
- [x] **Timestamped run outputs, latest-by-default** — done (#32): each run emits `behaviours-<ts>.json` + `manifest.json` (latest-by-default; `?data=` pin on the page, `select_run.py` on the CLI; committed `behaviours.json` is the fresh-clone fallback); user-generated files gitignored/unpushed
- [x] **How-to: "run it on your own behaviours"** — done, distributed across docs rather than one page: `engine/README.md` ("User specs"), `engine/panel/README.md` ("Behaviour metadata is registry-driven"), `specs/CITATION.md` (user-manifest pointer), root `AGENTS.md` (agent-facing pointers). Residual: no single standalone how-to page
- [ ] **Public page = small fixed demo + invitation** — the sample coverage stays a fixed demo that explicitly invites cloning/forking for one's own behaviours
- [ ] **Explicit limitations** — state what works vs. aspirational vs. unproven so people can judge whether/how to build on it (the endorsed part of option C)
- [x] **Structured stage-4 sidecar:** done (#31): `4-spec-coverage.json` emitted alongside the markdown, schema in `data/schema/spec-coverage-sidecar.schema.json`, `publish-coverage.py` prefers it (markdown regex fallback kept); behaviour 1's `research/sweeps/01-no-sycophancy/4-spec-coverage.json` reconstructed from its 20 published citations, so those records are regenerable. (The sidecar schema, the publisher, and the sweep records were later retired with the publish path; the published records remain in the frozen `data/coverage.json`, machine-verified in CI)
- [x] **Land CI verification:** landed in evolved form — `.github/workflows/ci.yml` runs the offline battery (panel/provenance/cite/registry suites, data gate, builder byte-identity, app.js harnesses) and the browser walkers on every PR, and `spec-watch/pull-latest.sh` aborts when upstream versions diverge from `cite.py`'s registry. The `publish-coverage.py --check` steps and `verify-spec-reader.mjs` named in the original item went with the publish path; the 0-byte archive fetch remains open
- [x] **Panel provenance on main:** done (#32): `engine/panel/runlog-v3.jsonl` committed + documented (`runlog-v3.md`) -- the #32 record; the shipped payload is now the v5 build, and `verify_panel_provenance.py` proves *that* payload rebuilds byte-identically from `runlog-v5.jsonl` (modulo the documented runDate allowance). Residual: the substitution provenance text is still a hand-edited string, not derived from the runlog
- [x] **Panel packaging + config injection:** done (#33): lazy injectable config (`harness.load_config()`; import-side-effect probe pins no file reads at import); explicit rubric-slot composition with frozen-prompt byte-identity tests; hermetic test subprocess env (no accidental spend)
- [ ] **Strip the vision/review-workflow documentation** that implies the internal authoring flow is the user path
- [ ] **Last decoupling item — convert the cite.py corpus goldens to hashes:** the full-corpus golden snapshots earn their repo weight while the contract is being refactored, because a failing golden is a reviewable byte-diff that shows exactly which locators a behavior change moves. Once the decoupling settles and the repo shifts from refactoring to maintenance, replace them with output hashes (a determinism/change-detection gate) and keep the locator re-resolution and corrupted-quote tests, which carry the published-citation contract independent of the goldens

## Phase 3 — Tier 1: extensible modular platform (adds to Phase 2)

> **Product if you stop here:** users bring their own specs/behaviours, produce coverage, and add local extensions — all without pushing back; the reader renders arbitrary user data.
> **Risk: high.** A redesign guessed without real users, executed on under-tested load-bearing code with no safety net — the most likely way to pick the wrong seams and break the demo. Strongly defer until there are real users to design the seams around.

- [ ] **Unify passage counting across the walkers:** the reader is one surface now, but `verify-reader-features.mjs` counts `.passage` cards (incl. continuation blocks) while `verify-reader-test.mjs` counts `[data-passage-id]` anchors; settle on one selector convention if the counts ever need to agree. (The ledger half is moot: the reader renders the v5 panel payload directly; there is no hand-transcribed ledger left to generate.)
- [ ] **Single source for agent-facing procedures:** the sweep procedures are retired with the publish path; the live agent-facing procedures are the root `AGENTS.md` and the engine READMEs. If a new procedure layer lands: one canonical copy, everything else a pointer (decide canonical location, pointer mechanism incl. the Windows-symlink caveat, and a CI sync check if copies are ever generated)
- [ ] **Generalize the site/reader to load arbitrary user payloads** — the readers currently read fixed committed payloads
- [ ] **Support local extensions** — the "add your own extensions that don't get pushed" model
